#include "main.h"
/**
 * _abs: Compute the absolute value of an integer
 *
 * @c: Input is an interger
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
